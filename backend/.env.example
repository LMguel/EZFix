# Exemplo de arquivo .env para desenvolvimento
# NÃO COMMITAR SEGREDOS REAIS. Substitua os placeholders pelas suas chaves locais.

# Servidor
PORT=3000

# JWT
JWT_SECRET=replace_with_a_strong_secret

# Banco de dados (Prisma) - por exemplo: postgresql://user:pass@localhost:5432/dbname
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

# Azure Computer Vision / Read API
AZURE_VISION_ENDPOINT=https://<your-region>.api.cognitive.microsoft.com
AZURE_VISION_KEY=replace_with_azure_vision_key

# OpenAI / Azure OpenAI
# If using Azure OpenAI set AZURE_OPENAI_ENDPOINT and AZURE_OPENAI_KEY
AZURE_OPENAI_ENDPOINT=https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_KEY=replace_with_azure_openai_key
AZURE_OPENAI_DEPLOYMENT=<your-deployment-name>
AZURE_OPENAI_API_VERSION=2024-11-22

# Or if using OpenAI public API
OPENAI_API_KEY=replace_with_openai_api_key
OPENAI_MODEL=gpt-4o-mini

# LLM fallback: permitir usar OpenAI público quando Azure bloquear por content_filter
LLM_ALLOW_OPENAI_FALLBACK=false

# Ativar formatação com LLM durante criação da redação (padrão: false)
LLM_FORMAT_ON_CREATE=false

# Prisma optional shadow DB for migrations (dev)
SHADOW_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/SHADOW_DB

# Any other env variables your local setup needs
# EXAMPLE_CUSTOM=valor
# Configuração do Banco de Dados PostgreSQL
DATABASE_URL="postgresql://postgres:ezfix@localhost:5432/ezfix"

# Segredo para JWT (use um valor forte em produção)
JWT_SECRET="seu-segredo-super-secreto-aqui"

# Porta do servidor (opcional, padrão: 3000)
PORT=3000